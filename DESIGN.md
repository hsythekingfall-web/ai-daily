# DESIGN.md — AI 前沿日报设计系统

> 本文件是全站唯一的视觉规范。任何模板/样式改动必须遵循此处定义的令牌与规则；
> 修改设计前先改本文件，再改代码。

## 1. 气质定位

温暖、安静、有刊物感的 AI 早报。低饱和马卡龙色纸面 + 宋体衬线标题 + 薰衣草紫点缀；
版式为"杂志开卷"式：卷首导语 → 按重要度的精选网格 → 分类栏目。克制、留白、不堆装饰。

## 2. 色板与角色

### 背景
- 页面渐变：`linear-gradient(165deg, #edf3fb 0%, #f0edfa 42%, #f7f0e2 100%)`
  （浅蓝 → 浅紫 → 浅黄，马卡龙三段渐变，只允许出现在 body，禁止再叠加其他底色）

### 文字
- 标题墨色 `#3b3663`（香芋紫墨，仅用于标题/刊名）
- 正文 `#555b74`；辅助说明 `#9095ad`；弱化信息 `#7d81a0`

### 强调
- 主强调（薰衣草紫）`#6c63d2`：链接、按钮、"必读/相关报道"章、悬停态
  （悬停加深 `#5a51c0`；页面中不得出现第二种按钮色）
- 马克笔划重点 `#f5e9b8`（仅用于卷首语中的关键词下划高亮）
- 重要度星点 `#d9a441`（赭金，全站唯一暖色，仅用于重要度）

### 分类色签（浅色底 + 同系深字，胶囊形）
| 分类 | 底色 | 字色 |
|---|---|---|
| 大模型 | `#e8e2f8` | `#6350b8` |
| 智能体 | `#ddeaf9` | `#2f6597` |
| 多模态 | `#fbe3ec` | `#b04a75` |
| 芯片与基础设施 | `#e2eef2` | `#33707e` |
| 开源项目 | `#ddf0e4` | `#2f7d54` |
| 政策与行业 | `#f7ecd0` | `#8a6a1c` |
| 研究前沿 | `#fbe0da` | `#b05b3f` |
| 行业动态(兜底) | `#eceaf4` | `#5c5c80` |

新增分类必须先在本表登记颜色后再上线。

## 3. 字体

- 衬线（标题/刊名/卷首语）：`'Songti SC', STSong, Georgia, 'Noto Serif SC', serif`，字重 700
- 无衬线（正文/UI）：`-apple-system, 'PingFang SC', 'Helvetica Neue', sans-serif`
- 不引入任何外部 webfont（离线可用）

### 字阶
| 角色 | 字体 | 规格 |
|---|---|---|
| Hero 主标题 | 衬线 | 40px / 700 / 行高 1.25 / 字距 -.5px |
| 栏目标题 | 衬线 | 26px / 700 / 居中 |
| 卡片标题 | 衬线 | 20px / 700 / 行高 1.4 |
| 页面标题(非首页) | 衬线 | 34px / 700 |
| 正文摘要 | 无衬线 | 14px / 行高 1.8 |
| 元信息/统计 | 无衬线 | 13px；徽章 12px / 600 |
| 卷首语导语 | 衬线 | 27px / 行高 1.55 |

## 4. 布局原则

- 页面骨架：全宽顶部导航 → 双栏主体(左侧粘性导航栏 176px + 主内容列) → 页脚；
  容器最大宽 1220px，左右内边距 32px；基础间距单位 8px
- 左侧导航栏 `position: sticky` 吸顶，随滚动用 IntersectionObserver 高亮当前栏目；
  ≤960px 时隐藏侧栏(移动端目录功能列入路线图)
- 首页结构：导航 → Hero(左文案 + 右悬浮早报卡) → 今日精选(宽卡 + 双列网格)
  → 各分类栏目(居中衬线标题 + 双列网格) → 页脚
- 非首页用"期号页头"(eyebrow 期号 + 衬线日期标题 + 统计行)替代 Hero
- 栅格：卡片网格 2 列、间距 20px；≤760px 折为单列

## 5. 组件规格

- **左侧导航栏**：分组小标题(12px/灰/加字距) + 链接行(14px、圆角 8px、
  内边距 7×12px)；行内可带 9px 分类色点(取第 2 节色签底色)；
  悬停底色 `#ecebf9`；当前栏目高亮 `#e9e6f9` 底 + `#5a51c0` 字 + 600 字重

- **导航**：印章式圆形 logo(渐变 `#8f86dd→#6c63d2`，白字"日报") + 衬线刊名；右侧文字链接
- **早报卡(Hero 右侧)**：白底、圆角 14px、描边 `#e6e3f2`、淡紫投影
  `0 12px 32px rgba(108,99,210,.10)`；内含橙色期号眉行、衬线标题、统计行、
  导读框(底 `#eef0fb`/描边 `#dcd9f0`/圆角 10px)、紫色"阅读今日早报 →"
- **资讯卡**：半透明白底 `#ffffffb8`、描边 `#e6e3f2`、圆角 14px、内边距 22×24px；
  徽章行(必读/相关报道 +N/分类色签) → 衬线标题 → 摘要 → 相关报道行 →
  分隔线(`#ece9f6`)上方放元信息、下方"阅读原文 →"
- **徽章**：胶囊 999px、12px/600；"必读""相关报道 +N"为紫底白字；
  分类色签见第 2 节
- **按钮**：主按钮紫底白字、圆角 8px、内边距 13×26px；次按钮白底描边 `#d8d5ec`
- **归档卡**：同资讯卡外壳；衬线日期 + 条数 + "查看 →"，悬停描边变紫

## 6. 该做与不该做

### 该做
- 同一分类永远使用第 2 节登记的色签，不得即兴配色
- 标题必须衬线、正文必须无衬线；正文行高 ≥ 1.7
- 每期必须有：期号、来源数/采集数/事件数统计行、重要度星点
- 悬停只做颜色变化(链接变紫)，不做缩放/阴影动画

### 不该做
- 不引入米黄/橙色等暖色大面积色块(暖色只允许赭金星点)
- 不使用渐变按钮、玻璃拟态、大面积阴影
- 不转载全文；卡片摘要超过 110 字必须截断
- 不在色签之外自造徽章颜色

## 7. 给 AI 编码代理的提示词

按本文件生成页面时遵循：先读第 2 节色板与分类色签表，卡片一律使用第 5 节规格；
中文标题用衬线，正文摘要 ≤110 字；所有外链 `target="_blank" rel="noopener"`；
生成后自查：分类色签是否登记、行高是否 ≥1.7、全页是否只有薰衣草紫一种强调色。

## 8. 路线图

- 移动端目录(侧栏折叠为横向滑动色签条)
- 暗色模式(夜间护眼)：基于同一布局，深底 `#171826`、卡片 `#1f2030`、文字 `#e8e9f5`
- 归档页年份分组、分类筛选
- Telegram/邮件推送模板复用本设计系统
